a、#{}是预编译处理，${}是字符串替换。

b、Mybatis 在处理#{}时，会将 sql 中的#{}替换为?号，调用 PreparedStatement 的 set 方法来赋值；#将传入的数据都当成一个字符串，会对自动传入的数据加一个双引号。
c、Mybatis 在处理${}时，就是把${}替换成变量的值。$将传入的数据直接显示生成在sql中。
d、使用#{}可以有效的防止 SQL 注入，提高系统安全性。
e,#是占位符，$是拼接符

　
 $一般多用于order by  MyBatis排序时使用order by 动态参数时需要注意，用$而不是#！
